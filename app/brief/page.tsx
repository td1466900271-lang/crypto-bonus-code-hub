import { Brand } from "../brand";
import { briefs } from "./data";
const latest=briefs[0];
export default function Brief(){return <main><header><a className="brand" href="/"><Brand/></a><nav><a href="/">首页</a><a href="/brief">市场简报</a></nav><a href="/">← 返回首页</a></header><section className="section"><p className="eyebrow">DAILY MARKET BRIEF · {latest.date}</p><h1 className="briefTitle">{latest.title}</h1><p className="lead">{latest.summary}</p><a className="posterLink" href={`/brief/${latest.date}`}><img className="briefPoster" src={`/brief-card/${latest.date}`} alt={`${latest.date} ${latest.title} 市场简报内容图`}/></a><div className="cards">{latest.points.map((x,i)=><article key={x}><p className="eyebrow">重点 0{i+1}</p><h2 className="briefPoint">{x}</h2></article>)}</div><section className="archiveSection"><p className="eyebrow">BRIEF ARCHIVE</p><h2>历史市场简报</h2><div className="briefArchive">{briefs.map((x,i)=><a href={`/brief/${x.date}`} key={x.date}><article><img src={`/brief-card/${x.date}`} alt={`${x.date} 简报内容图`}/><div><time>{x.date}</time><h3>{x.title}</h3><p>{x.summary}</p></div><span>{i===0?"最新":"查看 →"}</span></article></a>)}</div></section></section><footer><a className="brand" href="/"><Brand/></a><p>基于公开市场资料重新整理，仅供信息参考，不构成投资建议。</p><span>每日更新</span></footer></main>}
